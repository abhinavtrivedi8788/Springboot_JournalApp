spring:
  data:
    mongodb:
      uri: mongodb+srv://Dummy_User:Dummy_Passord@cluster0.foxflgi.mongodb.net/?retryWrites=true&w=majority
      database: journal_db
      auto-index-creation: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: mail Id
    password: 16 digits App password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.gmail.com

server:
  port: 8080

weather:
  api:
    key: Key
# Set the logging level to ERROR to reduce log verbosity
#logging:level:root: ERROR

# Set the logging level for the net.engineeringdigest.journal package to ERROR
#logging:level:net:engineeringdigest:journal: ERROR

# set the logging off for specific class
#logging:level:net:engineeringdigest:journal:service:UserService: OFF

